{
  "name": "backend-apps",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api"
  ],
  "scripts": {
    "dev": "npm --workspace=api run dev",
    "build": "npm --workspace=api run build",
    "start": "npm --workspace=api run start"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "ts-node": "^10.9.1"
  }
}

